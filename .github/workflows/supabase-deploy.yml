# Deploy Supabase — migrações SQL + Edge Functions.
#
# Disparo:
#   1. Manual: Actions → "Supabase Deploy" → Run workflow
#   2. Push em main quando mudam ficheiros em supabase/**
#   3. Agente Cloud: gh workflow run supabase-deploy.yml
#
# Secrets (Settings → Secrets and variables → Actions):
#   DATABASE_URL              — Connection string Postgres (pooler recomendado)
#   SUPABASE_ACCESS_TOKEN     — Personal Access Token (Account → Access Tokens)
#   SUPABASE_PROJECT_REF      — opcional se VITE_SUPABASE_URL estiver definido
#   VITE_SUPABASE_URL         — opcional (extrai project ref da URL)

name: Supabase Deploy

concurrency:
  group: supabase-deploy
  cancel-in-progress: false

on:
  workflow_dispatch:
    inputs:
      migrations:
        description: 'Aplicar migrações SQL pendentes'
        type: boolean
        default: true
      functions:
        description: 'Deploy Edge Functions'
        type: boolean
        default: true
      dry_run:
        description: 'Apenas listar o que seria feito (não altera nada)'
        type: boolean
        default: false
  push:
    branches: [main]
    paths:
      - '.github/workflows/supabase-deploy.yml'
      - 'supabase/migrations/**'
      - 'supabase/functions/**'
      - 'scripts/apply-supabase-migrations.mjs'
      - 'scripts/deploy-supabase*.mjs'

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Aplicar migrações SQL
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.migrations != false && inputs.dry_run != true) }}
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          set -e
          if [ -z "${DATABASE_URL:-}" ]; then
            echo "::warning::Secret DATABASE_URL em falta — migrações ignoradas."
            exit 0
          fi
          npm run db:apply-migrations

      - name: Dry-run migrações
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run == true && inputs.migrations != false }}
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          if [ -z "${DATABASE_URL:-}" ]; then
            echo "::warning::Secret DATABASE_URL em falta — dry-run migrações ignorado."
            exit 0
          fi
          npm run db:apply-migrations -- --dry-run

      - name: Deploy Edge Functions
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.functions != false && inputs.dry_run != true) }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
        run: |
          set -e
          if [ -z "${SUPABASE_ACCESS_TOKEN:-}" ]; then
            echo "::warning::Secret SUPABASE_ACCESS_TOKEN em falta — Edge Functions ignoradas."
            echo "Cria um PAT em https://supabase.com/dashboard/account/tokens e grava em GitHub Actions secrets."
            exit 0
          fi
          npm run supabase:deploy:functions

      - name: Dry-run Edge Functions
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run == true && inputs.functions != false }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
        run: |
          if [ -z "${SUPABASE_ACCESS_TOKEN:-}" ]; then
            echo "::warning::Secret SUPABASE_ACCESS_TOKEN em falta — dry-run functions ignorado."
            exit 0
          fi
          npm run supabase:deploy:functions -- --dry-run

      - name: Resumo deploy
        if: always()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          echo "DATABASE_URL: $([ -n "${DATABASE_URL:-}" ] && echo presente || echo EM FALTA)"
          echo "SUPABASE_ACCESS_TOKEN: $([ -n "${SUPABASE_ACCESS_TOKEN:-}" ] && echo presente || echo EM FALTA)"
          echo "PROJECT_REF: $([ -n "${SUPABASE_PROJECT_REF:-}${VITE_SUPABASE_URL:-}" ] && echo presente || echo EM FALTA)"
